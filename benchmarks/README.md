# Benchmarks

Run the deterministic parser benchmark with:

```bash
cargo bench -p universal-payment-qr-core
```

Benchmark inputs are synthetic/public test vectors. No real payment data is included.

